# docker abbrevations
abbr -a dc "docker-compose"
abbr -a dk "docker"
abbr -a dkps "docker ps -a"
abbr -a dkrm "docker rm (docker ps -a -q)"
abbr -a dkrmi "docker rmi (docker images -q)"
abbr -a dm "docker-machine"
abbr -a dmev "eval (docker-machine env default)"
